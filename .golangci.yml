linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

linters-settings:
  errcheck:
    exclude-functions:
      - (github.com/pterm/pterm.SpinnerPrinter).Stop
      - (github.com/pterm/pterm.TablePrinter).Render
      - (*gopkg.in/ini.v1.Section).NewKey
      - (*gopkg.in/ini.v1.Section).DeleteKey

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck

